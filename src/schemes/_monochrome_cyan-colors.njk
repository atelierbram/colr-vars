{% import "macros/macro-sass.njk" as macroSass %}// monochrome_cyan
$monochrome_cyan-colors: (
{% for sass in tiles_monochrome_cyan -%}
  {{ macroSass.sassMacro(sass.tile_id, sass.tile_hsl) }}
{%- endfor -%}
);
